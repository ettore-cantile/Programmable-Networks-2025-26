# Configure MAC address to match static switch routes
ip link set eth0 address 00:00:00:00:03:03
# Configure IP address
ip addr add 10.0.3.1/24 dev eth0
# Set default gateway
ip route add default via 10.0.3.254
# Add static ARP entry for the edge switch H
arp -i eth0 -s 10.0.3.254 00:00:00:00:08:00