# Programmable Networks A.Y. 2025/26

This repository contains the laboratory assignments for the PNET ( 2025/26 ) course.

### 👥 Authors
**Ettore Cantile** ( 2026562 ), **Leonardo Chiarparin** ( 2016363 ), **Pierluca Grasso** ( 1950186 )

---

## Homework 1: ML Training Incast Orchestration

### 📝 Project Overview and Scenario
The exponential growth of distributed Machine Learning ( ML ) workloads in modern data centers presents significant network orchestration challenges. In this scenario, the network topology, a multi-tier spine-leaf architecture modeled via Open vSwitch ( OVS ), hosts multiple synchronized distributed ML training jobs. Within each training group, a cluster of dedicated "Workers" concurrently transmit large parameter updates and tensor datasets to a designated centralized "Collector" node. 

### 🌊 Nature of the Traffic
The traffic pattern generated by this architecture is highly synchronized, parallel, and bursty, leading to a severe network phenomenon known as the **TCP Incast problem**. During each training cycle (or round), multiple workers simultaneously initiate high-bandwidth TCP flows destined for the same collector interface. This many-to-one communication pattern creates massive micro-bursts that rapidly saturate the switch buffers on the edge links connecting to the collectors. The resulting packet drops ( tail drop ) force the TCP congestion window to collapse, triggering expensive Retransmission Timeouts ( RTOs ) and catastrophic throughput degradation, which drastically inflates the overall training completion time.

### 🎯 Objective
The primary objective of this project is to mitigate TCP Incast congestion and optimize distributed training performance by deploying a **Capacity-Aware SDN Orchestrator**. 
Leveraging a custom **POX controller** ( `project_1.py` ), the system dynamically executes the following tasks:

1. **Topology & Host Discovery:** Automatically discovers network links and tracks host locations dynamically using ARP proxying and IP interception.

2. **Traffic Characterization:** Monitors real-time flow counters from the switches to characterize burst thresholds and estimate bandwidth consumption.

3. **K-Shortest Paths Routing:** Computes multiple redundant paths using a Depth-First Search ( DFS ) algorithm bounded by a configurable hop cutoff.

4. **Capacity-Aware Load Balancing:** Intelligently distributes individual worker flows across distinct spine-leaf paths based on the virtual allocation rates of the links, ensuring that aggregated traffic does not breach physical capacity limits.

5. **Dynamic Reset:** Continuously tracks training cycle completions, clears expired flow rules via hard timeouts, and fully resets environmental states after a specified period of inactivity.

The traffic generation, lifecycle synchronization, and performance plotting are handled externally via an automated simulation script ( `incast_generator.py` or `incast_generator_2.py` ) utilizing `iperf3`.

---

### 🚀 How to Run the Laboratory

To execute the network simulation and trigger the orchestration, you must strictly follow this sequence:

1. **Start the Kathara Infrastructure:**
Open your host terminal in the `lab_project` root directory and launch the network topology:

    ```bash
    kathara lstart --noterminals
    ```

2. **Connect to the Controller Node:**
Open a shell connection into the controller container:

    ```bash
    kathara connect controller
    ```

3. **Launch the POX Controller:**
Inside the controller container, initialize the POX execution framework along with the openflow discovery module and the custom orchestrator:

    ```bash
    /pox/pox.py openflow.discovery project_1
    ```

4. **Wait for Topology Stabilization:**
Monitor the controller log stream. Do not trigger traffic prematurely. Wait until the internal discovery module maps all interfaces and explicitly prints the following stabilization anchor: `[SYSTEM] Topology stabilized! The incast generator can be launched.`

5. **Trigger the Traffic Generator:**
Once the stabilization log appears, open a new terminal window on your host machine ( inside the `lab_project` root folder ) and execute the desired traffic script:

    ```bash
    python3 incast_generator.py
    ```

6. **Terminate the Laboratory:**
Once the experiments are concluded and the performance graphs have been successfully generated, safely shut down the simulation environment:

    * Halt the POX controller by pressing `Ctrl + C` inside its terminal, then type `exit` to return to your host machine shell.

    * Verify that no residual background processes associated with the traffic generator remain active ( if necessary, forcefully terminate them using a command such as `sudo killall iperf3` or your OS equivalent ).

    * Finally, completely tear down the virtual infrastructure and wipe the network topology by executing: `kathara lclean`

---

### ⚠️ Important Note for Windows Users

This network laboratory framework was natively developed, optimized, and validated on a **native Linux distribution ( Ubuntu )**. 

If you or your evaluator execute this laboratory inside **Windows ( WSL2, Docker Desktop )**, a critical kernel-level virtualization issue will arise: the switches will fail to connect or maintain stability with the POX controller ( `is_connected: true` will not be achieved ).

* **The Cause:** This failure stems from an environment-specific bug involving **TCP Checksum Offloading** inside virtualized hypervisor networks ( such as Microsoft's WSL2 kernel or Docker's LinuxKit proxy layer ). The virtual network stack miscalculates the TCP checksum sequence for OpenFlow protocol packets, forcing the POX controller to reject them silently as corrupted traffic. Moreover, in some environments, the required tunnel device node ( `/dev/net/tun` ) is not automatically created, preventing Open vSwitch from creating virtual cables.

* **Linux Virtual Machines:** Executing this laboratory within a full hardware-emulated Virtual Machine ( e.g., Oracle VirtualBox or VMware ) running a Linux guest OS does not present these anomalies. These hypervisors emulate complete hardware components and utilize a pure native Linux kernel, which correctly calculates packet checksums.

* **Kathara Plugin Modification Warning:** Due to a specific modification in the internal Kathara infrastructure settings, the Docker network plugin has been migrated from `katharaarp_vde` to `katharaarp`. Because of this architectural transition, executing this laboratory framework on operating systems that are not specifically native or optimized for this exact plugin modification is highly discouraged, as it will induce profound routing anomalies and system instability.

* **The Solution:** To guarantee cross-platform execution, you must implement a two-step workaround on the management interfaces of all devices ( the controller and every single switch ). Ensure that inside **each** `.startup` file, the following conditional block is placed right below the IP address assignment:

    ```bash
    if grep -qiE 'microsoft|linuxkit' /proc/version; then
        
        # Restore missing TUN/TAP device needed by Open vSwitch
        mkdir -p /dev/net
        mknod /dev/net/tun c 10 200 2>/dev/null || true
        
        # Disable TCP Checksum Offloading to prevent OpenFlow and TCP packet corruption
        ethtool -K ethX tx off rx off
        
    fi
    ```

    *( Make sure to replace `ethX` with the management interface of that specific node, such as `eth0` for the controller, `eth12` for `l1`, etc. )*

---

### 📊 Dependencies and Graphical Plotting

The automated traffic orchestration scripts ( `incast_generator.py` and `incast_generator_2.py` ) rely on the **Matplotlib** library to track throughput and render bandwidth allocation curves upon test completion.

* **Installation:** Ensure the library is installed on your host machine Python environment:

    ```bash
    pip install matplotlib
    ```

* **Headless / Non-Interactive ( SSH ) Environments:** If you run the traffic scripts in an environment lacking an active graphical display server ( e.g., a remote Linux server via SSH, or a terminal environment without an active X11/Wayland display ), the interactive GUI rendering command `plt.show()` will crash the execution thread. To prevent this, edit the plotting sections within both `incast_generator.py` and `incast_generator_2.py`, replacing all instances of:

    ```bash
    plt.show()
    ```

    with a persistent file saving directive:

    ```bash
    plt.savefig( "figure.png" )
    ```

    *( This modification safely writes the performance graphs directly into the local directory as PNG images. )*

---

## Homework 2: [ Placeholder ]

*( The documentation, scenario description, execution guidelines, and objective parameters for Homework 2 will be appended here once the assignment details are published. )*